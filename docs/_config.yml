# Site settings
title: TJ Smith's Pages
description: A placeholder for files and stuff
email: tjsmith@switchboxinc.com
baseurl: ""
url: ""

# Build settings
theme: jekyll-theme-minimal
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
```

The key changes:
- ✅ Removed `permalink: /:year/:title/` — this was breaking all your URLs
- ✅ Removed the duplicate `theme:` line
- ✅ Fixed the double apostrophe in `TJ Smith''s` (was `''`, should be `'`)

After committing, wait 1-2 minutes for the deploy to run, then try:
```
https://tbirdosu.github.io/capra-crosswalk.html
